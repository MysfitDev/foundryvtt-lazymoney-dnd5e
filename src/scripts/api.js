const API = {
  // XXXArr(...inAttributes) {
  // 	// if (!Array.isArray(inAttributes)) {
  // 	//   throw error('XXXArr | inAttributes must be of type array');
  // 	// }
  // 	this.XXX();
  // },
  // XXX() {
  // },
};

export default API;
